# Superseded runtime evidence — not the final visual set

These captures document the first complete Chrome WebMCP journey before the final anti-slop and accessibility fixes. They remain for traceability, but they are stale.

Use [`../final-zero-slop/`](../final-zero-slop/) for current connected, unavailable, diagnosis, refusal, proposal, transfer, zoom, reduced-motion, landing, and hover evidence.
