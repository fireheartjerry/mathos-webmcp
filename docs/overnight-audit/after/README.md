# Historical hostile-QA evidence — not the submission set

This directory intentionally preserves screenshots of defects discovered during the rescue, including files whose names begin with `BUG`. They are evidence that the audit was adversarial; they are **not** current product screenshots and must not be submitted as the final candidate.

The authoritative current set is [`../final-zero-slop/`](../final-zero-slop/), captured from the rebuilt candidate on 2026-08-27.
