# Baseline score — MathBurst-WebMCP-Challenge-Baseline.mp4

Scored independently against `webmcp-film-criteria-for-scorer.md` (Score log / Baseline fields not present in the copy I received). Artifact probed: 177.233 s, H.264 2560×1440 60 fps + AAC 48 kHz stereo, 26,304,807 bytes. Audio mean −22.6 dBFS, peak −4.6 dBFS (pre-computed). Inspection method: ffprobe data, supplied contact sheet, plus my own frame grabs at 0.5/3/8/14/170/174/176.5 s and every 6 s (30 frames), read at full resolution; narration reconstructed from the audio track's transcript.

## Score: 81.5/100

## Gates
| Gate | Result | Evidence |
|------|--------|----------|
| G1 | PASS | 177.233 s < 180 s; H.264/AAC master decodes cleanly; no third-party music or material detected in audio/frames. |
| G2 | FAIL | The URL `https://mathburst.fireheartjerry.chatgpt.site/` is never visible. No browser chrome or address bar appears in any sampled frame (opening gallery, mid-film, or the closing "One mathematical world" hold at 170–177 s). The film shows only the app surface fullscreen; a judge could not read any URL from it. |
| G3 | PASS | At ~8 s the cursor opens "Create a project" from the gallery, types **Pipeline** into PROJECT TITLE, and clicks "Create project →" before any math begins (frame key_8). |
| G4 | PASS | Human actions are cursor-led throughout (visible cursor in gallery, modal, canvas, sliders). Tutor actions carry conspicuous named toasts ("Used 2 WebMCP tools: inspect_math, focus_objects" at 78 s and 144 s; "step_history / step_history / delete_objects" chips at 162 s), purple "Tutor" cursor labels, and a left-edge ledger ("12/48 used" → "21/48 used"). A purple sweep/frame surrounds the active card when the tutor acts (78 s). |
| G5 | FAIL | Status badge and named toasts sit top-centre/top-left and the n/48 totals ledger is on the left edge, but the action **log** ("Activity" panel — "Every human and tutor action will land here.") is pinned bottom-**right** in every sampled frame (24 s, 60 s, 78 s, 132 s, 170 s). The gate requires status, totals, toasts, **and log** on the left; the log's placement violates it on every shot where it appears. |
| G6 | PASS | Claims match capture: "48 live page tools" badge matches "48 tools registered" toast/narration; the used-counter (21/48 at close) is consistent with the events shown; undo is demonstrated on a tutor action; Activity panel attributes Tutor vs You consistently with the narration ("Tutor raised the shape a to 5.5 / You moved the CDF bound"). No 48-call exercise is claimed, so that clause does not apply. |
| G7 | FAIL | Most beats map to visible time: approval ("Approved the clean reconstruction", Activity 60 s), typewriter LaTeX and resize (~44–57 s, "Resized objects"), density construction and morph (57–71 s; triangle morph overlay at 114 s), attention edit + one tutor step undone + eight student steps (71–89 s), barycentric triangle (89–102 s), geometry with dependency-order construction (102–111 s), shapes/ink/eraser parity (111–133 s), partitions/Ramanujan lane (140–144 s, frame f025), animated matrix (~145–157 s), editing + triple undo (157–167 s). Two required beats do **not** land: **isolation** is not visible anywhere I inspected, and the close shows "21/48 used" + "48 tools registered" — there is no **48/48** counter moment. |
| G8 | FAIL | One clear crop/overflow defect: at ~78 s (frame f014) the training card overflows the viewport — the header "One honest training step" is cut at the top and the left column is clipped ("W_Q" reads as "V_Q", "WEIGHTS BEFORE" reads as "EIGHTS BEFORE"). Other sampled frames and the contact sheet show no black flashes or blank frames, and the closing hold is present, but the gate says no overflow/truncation, and this is visible truncation of proof content. |

## Per-criterion
| # | Criterion | Points | Max | Evidence |
|---|-----------|--------|-----|----------|
| C1 | WebMCP leverage | 15 | 18 | Seven of eight proof types simultaneously visible across the film: registration (badge "48 live page tools"), named calls (toasts naming `inspect_math`, `focus_objects`, `step_history`, `delete_objects`), aura (purple Tutor cursor labels + purple card sweep), reducer commit (Activity entries per action), editable result (X₁ marked `editable`, W_Q edited to 1.4), shared history (step_history calls; one Activity feed mixing Tutor/You), undo (Undo button; undo of tutor eraser narrated and logged). Isolation — the eighth — is never shown. Between anchors 9 (four types) and 18 (all eight), much nearer 18. |
| C2 | Human/agent parity | 14 | 16 | Both sides explicit for most named categories: pen/ink (student writes, tutor marks), equation type/resize (tutor types LaTeX, "same handles you would drag"), sliders (Activity: "Tutor raised the shape a" vs "You moved the CDF bound"), training (1 tutor step undone, then 8 student steps), shapes (tutor ellipse via create_shape, student rotates it by handles), ink/eraser (student erases; tutor erases own circle; undo restores), matrix (typed by hand, shared by tool), editing (graph/construction/edit + 3 undos). Weaker: project creation is human-only (agent never creates/duplicates a project), and geometry construction is student-places/auto-constructs rather than a matched pair. |
| C3 | Connected journey | 12 | 14 | The chain gamma recurrence → density → softmax → attention → barycentrics → geometry → partitions/Ramanujan → matrix reconstructs after one pass, and bridges are on screen, not just narrated: "log-masses are the logits the attention head starts from" printed on the density card (60 s); "the same three numbers arrived at twice" bridging to attention; "weights that sum to one, like attention weights" printed under the barycentric scene (visible in closing thumbnail row). Not every act motivates the next visually — the jump into Olympiad geometry and then into partitions is carried mostly by narration — so it sits between anchors 7 and 14, nearer 14. |
| C4 | Animated construction | 11 | 14 | Density builds in dependency order (axes → curve → three mass bins, 57–71 s) with tool-attributed steps; morph of density into a triangle overlay is visible at 114 s; attention weights move live while summing to one (78 s); geometry homothety/spiral-center construct in dependency order (102–111 s); partition lane highlights values; matrix animates all vectors at once. I could confirm staged construction with purple attribution in four of the six named sequences at full resolution; the simplex/partition and matrix sequences are dark-scene and compressed, and I could not verify their polish frame-by-frame. Between 7 and 14, nearer 14. |
| C5 | Challenge story & impact | 11 | 12 | First 40 s answer: product (shared math whiteboard for student + AI tutor, on-screen subtitle), problem ("a tutor can only talk about your work… it explains around the problem instead of touching it"), mechanism ("registers 48 WebMCP tools… one reducer, one history"). Last 20 s answer: new capability (every tutor edit went through page tools; student reached the same reducer; one history) and ambition ("One mathematical world. Every agent can enter."). All five answers form one thesis; docked slightly because the deployment/URL leg of the story is invisible (see G2). |
| C6 | Pacing & sync | 7 | 10 | 16 narration segments spread evenly over 177 s; visuals I sampled land with their lines (density words over density build at 57–71 s; undo line over undo at 121–133 s). Flags: the opening gallery + modal runs ~12 s of low-information footage before the math begins; the 162–177 s closing hold is long and static. I sampled frames plus narration timings rather than a continuous 1× watch, so minor rushed/late moments inside dense segments (78–89 s) could not all be verified — scored on the strict side. |
| C7 | Visual composition | 5 | 7 | Most sampled frames are deliberate: clear focal action, visible cursor, stable geography, legible math at 1440p (gamma ink, density table, partition table all crisp). Flags: the 78 s training card is cropped at top and left (see G8); wide zoom-outs (132 s, 170 s) shrink earlier proof to thumbnail size; the left-edge vertical "n/48 used" ledger is rotated 90° and easy to miss. Between 3.5 and 7. |
| C8 | Audio craft | 3.5 | 5 | Measured mean −22.6 dBFS / peak −4.6 dBFS — safe peaks, no clipping, dialogue-forward level; narration transcribes cleanly end-to-end with no clipped lines. I could not listen on headphones in this environment, so naturalness, music ducking, SFX, and seam quality are verified only indirectly (clean transcript, sane levels). Between 2.5 (clean but flat) and 5 (balanced and seamless); without direct listening I score at the lower-middle. |
| C9 | Technical delivery & close | 3 | 4 | Technical: valid 2560×1440 60 fps H.264/AAC, decodes fully, thin math lines (integrals, subscripts, Ferrers diagram) survive compression. Close: "One mathematical world. Every agent can enter." holds ~7 s with the "48 tools registered" toast — memorable and recalable. But the required public-URL hold is absent (G2), so the "both" anchor (4) is not met; above 2 because technical delivery is clean and the close is otherwise strong. |

## Unscoreable
None — every criterion had at least partial direct evidence; where a check could only be partially performed (continuous 1× viewing for C6, headphone listening for C8) I scored on the evidence available and flagged it in the criterion notes rather than marking N/A.

## Notes
- G2 is the most damaging finding and also the cheapest to fix: no frame — opening or closing — ever shows a URL or browser chrome, so "public deployed surface" is asserted nowhere a judge can see.
- G5 hinges on treating the bottom-right "Activity" panel as "the log". It self-describes as the action log ("Every human and tutor action will land here."), so I scored the gate strictly as FAIL. If the criteria intend only the left-edge n/48 ledger as "totals" and don't care where Activity lives, the gate text should say so.
- G7: "isolation" is the one beat I could not map to any visible time in 30 sampled frames + full narration transcript; and the close is "48 registered / 21 used", not "48/48". If "48/48" is meant literally as a counter, it is absent; if "48 tools registered" satisfies it, the gate wording needs softening.
- G8: single-instance crop at ~78 s. Binary gate, so one defect fails it; the rest of the boundary audit (30 frames + contact sheet) was clean.
- C6/C8 carry method caveats (no continuous 1× watch, no listening); both were scored slightly strict per the ambiguity rule. A scorer with playback+audio could move C6 by ±2 and C8 by ±1.5.
